# GPU / heavy OCR backends — install on the GPU server only.
#   pip install -r requirements.txt -r requirements-gpu.txt
# Install a CUDA build of torch matching your driver first, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
torch>=2.3
transformers>=4.49,<5   # Qwen2.5-VL OCR + transformers answer LLM; <5 for FlagEmbedding reranker compat
accelerate>=0.30
# High-throughput local LLM serving (alternative to in-process transformers):
#   run a vLLM/Ollama OpenAI-compatible server and set LLM_BACKEND=openai
# vllm>=0.6
qwen-vl-utils>=0.0.8    # image preprocessing for Qwen2.5-VL
surya-ocr>=0.6          # alternative OCR backend (multilingual incl. Arabic)
# Optional high-throughput serving of the Qwen2.5-VL OCR model:
# vllm>=0.6
