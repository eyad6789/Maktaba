# Core runtime deps (CPU-friendly; works for local dev). GPU/OCR-heavy backends
# live in requirements-gpu.txt. Install with: pip install -r requirements.txt
pymupdf>=1.24            # PDF text extraction + page rendering (import: fitz)
pillow>=10.0            # image handling for OCR
qdrant-client>=1.12     # vector DB client (dense + sparse)
openai>=1.40            # client for a LOCAL OpenAI-compatible LLM (vLLM/Ollama/LM Studio)
anthropic>=0.49         # OPTIONAL: only if llm_backend="anthropic" (cloud, off by default)
fastapi>=0.115          # serving API
uvicorn[standard]>=0.30 # ASGI server
python-multipart>=0.0.9 # multipart parsing for the /upload endpoint
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0
redis>=5.0              # queue backend
rq>=1.16               # simple job queue for batch ingestion
numpy>=1.26
tqdm>=4.66
langdetect>=1.0.9       # lightweight language detection (ar/en)
FlagEmbedding>=1.2.10   # BGE-M3 embeddings + bge-reranker-v2-m3 (pulls torch)
transformers>=4.49,<5   # pulled by FlagEmbedding; v5 removed the slow-tokenizer
                        # `prepare_for_model` the reranker uses -> pin <5
pytesseract>=0.3.10     # fallback OCR backend (needs system `tesseract` + ara/eng data)
